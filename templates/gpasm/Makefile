# This Makefile was generated by the made script:
# https://github.com/alx741/made
#
# GNU PIC Assembler - For Microchip's PIC microcontrollers
#
# Replace FILE with your ASM code filename
#
# Dependencies:
# 	gputils


FILE=your_file

$(FILE).hex: $(FILE).asm
	gpasm $<

clean:
	rm $(FILE).hex $(FILE).cod $(FILE).lst
